'use client'

/**
 * Site-wide footer — official MUI Marketing multi-column footer.
 * Kept as Footer export so all existing page imports keep working.
 */
export { default } from '@/components/marketing/MarketingFooter'
