'use client'

/**
 * Site-wide header — official MUI Marketing floating AppBar.
 * Kept as Header export so all existing page imports keep working.
 */
export { default } from '@/components/marketing/MarketingAppBar'
